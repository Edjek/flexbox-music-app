import Game from './Game';
import Result from './Result';

const Page = () => {
    return (
        <section className="container">
            <Game />
            <Result />
        </section>
    );
};

export default Page;
